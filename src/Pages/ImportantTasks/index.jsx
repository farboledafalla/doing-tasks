// Layout
import { Layout } from '../../components/Layout';

// Components
import { Search } from '../../components/Search';

export const ImportantTasks = () => {
   return (
      <Layout>
         <Search />
      </Layout>
   );
};
