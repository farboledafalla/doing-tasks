// Layout
import { Layout } from '../../components/Layout';

// Components
import { Search } from '../../components/Search';

export const Lists = () => {
   return (
      <Layout>
         <Search />
      </Layout>
   );
};
