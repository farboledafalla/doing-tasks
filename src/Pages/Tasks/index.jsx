// Layout
import { Layout } from '../../components/Layout';

// Components
import { Search } from '../../components/Search';

export const Tasks = () => {
   return (
      <Layout>
         <Search />
      </Layout>
   );
};
