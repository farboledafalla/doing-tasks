// Layout
import { Layout } from '../../components/Layout';

// Components
import { Search } from '../../components/Search';

export const Home = () => {
   return (
      <Layout>
         <div>Home</div>
         <Search />
      </Layout>
   );
};
